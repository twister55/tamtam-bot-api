{
  "name": "examples",
  "version": "1.0.0",
  "description": "Examples in typescript so first of all you need install dependencies and compile it to javascript",
  "private": true,
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^14.14.20",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "form-data": "^3.0.0",
    "tamtam-bot-api": "^0.3.1"
  }
}
